# RSI Strategy Analyzer (Streamlit)

Interactive Streamlit app for backtesting RSI-based trading strategies on multiple markets using **yfinance** data.

---

## Requirements
Python 3.9+ and the libraries listed in `requirements.txt`.

Install dependencies:
```bash
pip install -r requirements.txt
